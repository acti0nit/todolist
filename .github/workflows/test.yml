##
# Run unit test and the action itself
##
name: Tests
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  tests:
    steps:
    - uses: acti0nit/todolist/.github/workflows/checks/test.yml@master
  dist:
    steps:
    - uses: acti0nit/todolist/.github/workflows/checks/dist.yml@master
  codeql:
    steps:
    - uses: acti0nit/todolist/.github/workflows/checks/codeql.yml@master
